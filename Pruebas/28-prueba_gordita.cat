Reino gordita ~

Comportamientos
    entrenar dummy ~ plancton delfin
~~
Nacimiento
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("Estamos probando") ~ 
    rugirSerpiente("-----------------------------------------------------------------------") ~ 
    rugirSerpiente("FIN DE LA PRUEBA...") ~ 
Muerte gordita ~~